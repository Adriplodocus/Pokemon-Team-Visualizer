# Backward compat: old Showdown sprite URLs (pre-theme-reorganization)
# Static assets (Home/Official/Pixel/Showdown PNGs/GIFs) are served directly
# and bypass these rules. Only missing/moved files hit the redirect.
/sprites/* /sprites/Showdown/:splat 301
